@model ES_WEBKYSO.Models.D_DOIGCS

<div style="margin: -15px;">
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">&times;</button>
        <h4 class="modal-title">@ViewBag.Title</h4>
    </div>
    @using (Html.BeginForm("Create", "DanhMucDoi", FormMethod.Post, new { @class = "form-horizontal", name = "frm", id = "frm" }))
    {
        <div class="modal-body">
            @Html.AntiForgeryToken()
            @*@Html.HiddenFor(x => x.MA_DVIQLY)*@
            <div class="form-group focus">
                @Html.EditorFor(x => x.MA_DVIQLY, new { htmlAttributes = new { @class = "form-control", @Value = ViewBag.MaDviQly, @readonly = "readonly" } })
                @Html.EditorFor(model => model.MA_DOIGCS, new{ htmlAttributes = new{ @class = "form-control"}, autofocus = "autofocus"})
            </div>
            <div class="form-group">
                @Html.EditorFor(x => x.TEN_DOI)
                @Html.EditorFor(x => x.GHI_CHU)
            </div>
        </div>

        <div class="modal-footer">
            @*<button type="button" class="btn btn-primary" id="DanhMucDoiCreate">Cập nhật</button>*@
            <button type="submit" class="btn btn-primary" id="DanhMucDoiCreate">Lưu lại</button>
            <button type="button" class="btn btn-default" data-dismiss="modal" id="DanhMucDoiClose">Đóng</button>
        </div>
    }
</div>

<script type="text/javascript">
    $(document).ready(function () {
        $("#myTextBoxId").focus();
    });
    $(document).ready(function () {
        $.validator.unobtrusive.parse("#frm");
    });
    $(function () {
        $(".focus :input").focus();
    });
    @*$(document).ready(function () {
        $("#DanhMucDoiCreate").on('click', function (e) {
            e.preventDefault();
            $.ajax({
                type: 'POST',
                cache: false,
                async: true,
                url: "@Url.Action("Create", "DanhMucDoi")",
                dataType: 'json',
                data: $("#DanhMucDoiCreateForm").serialize(),
                success: function (result) {
                    if (result.status === false) {
                        alert(result.messenger);
                    }
                    if (result.status === true) {
                        alert(result.messenger);
                        $("#DanhMucDoiClose").click();
                    }
                },
                error: function (jqXHR, textStatus, errorThrown) {

                },
                complete: function (result) {
                    window.TableGenericDanhMucDoiGrid.ajax.reload();
                }
            });

        });*@

    $('#popupStaticModal').on('hidden.bs.modal', function () {
        $("#popupStaticModal").html('<div class="modal-body"><div></div></div>');
    });

    //});

</script>

@section PageScripts{
    <script src="~/Scripts/jquery.validate.js"></script>
    <script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
}